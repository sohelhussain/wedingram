
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wedding App</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans leading-normal tracking-normal">

    <!-- Navbar -->
    <nav class="bg-white shadow">
        <div class="container mx-auto px-6 py-3">
            <div class="flex justify-between items-center">
                <div>
                    <a href="#home" class="text-xl font-bold text-gray-800 hover:text-gray-600">Wedding App</a>
                </div>
                <div class="hidden md:flex space-x-6">
                    <a href="#home" class="text-gray-800 hover:text-gray-600">Home</a>
                    <a href="#about" class="text-gray-800 hover:text-gray-600">About</a>
                    <a href="#features" class="text-gray-800 hover:text-gray-600">Features</a>
                    <a href="#contact" class="text-gray-800 hover:text-gray-600">Contact</a>
                    <button id="loginBtn" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">Login</button>
                    <button id="registerBtn" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600 transition">Register</button>
                </div>
            </div>
        </div>
    </nav>

    <!-- Home Section -->
    <section id="home" class="bg-white py-20">
        <div class="container mx-auto px-6 text-center">
            <h2 class="text-4xl font-bold text-gray-800">Welcome to Wedding App</h2>
            <p class="mt-4 text-gray-600">Plan your wedding effortlessly and beautifully.</p>
            <button class="mt-6 px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-500">Get Started</button>
        </div>
    </section>

    <!-- About Section -->
    <section id="about" class="bg-gray-200 py-20">
        <div class="container mx-auto px-6 text-center">
            <h2 class="text-4xl font-bold text-gray-800">About Us</h2>
            <p class="mt-4 text-gray-600">We provide all the tools you need to make your wedding planning smooth and joyful.</p>
        </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="bg-white py-20">
        <div class="container mx-auto px-6">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div class="bg-gray-100 p-6 rounded-lg shadow-md text-center">
                    <h3 class="text-2xl font-semibold text-gray-800">Feature One</h3>
                    <p class="mt-4 text-gray-600">Description of feature one.</p>
                </div>
                <div class="bg-gray-100 p-6 rounded-lg shadow-md text-center">
                    <h3 class="text-2xl font-semibold text-gray-800">Feature Two</h3>
                    <p class="mt-4 text-gray-600">Description of feature two.</p>
                </div>
                <div class="bg-gray-100 p-6 rounded-lg shadow-md text-center">
                    <h3 class="text-2xl font-semibold text-gray-800">Feature Three</h3>
                    <p class="mt-4 text-gray-600">Description of feature three.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="bg-gray-200 py-20">
        <div class="container mx-auto px-6 text-center">
            <h2 class="text-4xl font-bold text-gray-800">Get in Touch</h2>
            <p class="mt-4 text-gray-600">We would love to hear from you! Please reach out for any queries or further information.</p>
            <form class="mt-8 max-w-md mx-auto">
                <input class="w-full p-3 rounded-lg shadow-md mb-4" type="text" placeholder="Your Name">
                <input class="w-full p-3 rounded-lg shadow-md mb-4" type="email" placeholder="Your Email">
                <textarea class="w-full p-3 rounded-lg shadow-md mb-4" placeholder="Your Message" rows="4"></textarea>
                <button class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-500">Send Message</button>
            </form>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-white py-6">
        <div class="container mx-auto px-6 text-center">
            <p class="text-gray-600">© 2024 Wedding App. All rights reserved.</p>
        </div>
    </footer>

    <!-- Modal (hidden by default) -->
    <div id="authModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden">
        <div class="bg-white p-8 rounded-lg w-80">
            <h3 id="modalTitle" class="text-xl font-semibold mb-4">Sign In</h3>
            <input type="text" placeholder="Email" class="border border-gray-300 p-2 w-full mb-4">
            <input type="password" placeholder="Password" class="border border-gray-300 p-2 w-full mb-4">
            <button class="bg-blue-600 text-white px-4 py-2 rounded w-full hover:bg-blue-700 transition">Submit</button>
            <button id="closeModal" class="mt-4 text-blue-600 hover:text-blue-800 transition">Cancel</button>
        </div>
    </div>

    <script>
        const loginBtn = document.getElementById('loginBtn');
        const registerBtn = document.getElementById('registerBtn');
        const authModal = document.getElementById('authModal');
        const modalTitle = document.getElementById('modalTitle');
        const closeModal = document.getElementById('closeModal');

        loginBtn.addEventListener('click', () => {
            modalTitle.textContent = 'Sign In';
            authModal.classList.remove('hidden');
        });

        registerBtn.addEventListener('click', () => {
            modalTitle.textContent = 'Register';
            authModal.classList.remove('hidden');
        });

        closeModal.addEventListener('click', () => {
            authModal.classList.add('hidden');
        });
    </script>
</body>
</html>
